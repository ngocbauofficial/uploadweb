@model ProductDetailsModel
@using Nop.Web.Models.Catalog;
@if (Model.HasSampleDownload)
{
    <div class="vehicle-call-to-action mr-2">
        <a class="Link Link-download_brochure_terra" href="@Url.RouteUrl("GetSampleDownload", new { productid = Model.Id })">
            <small>
                Tải
                về
            </small> Brochure
        </a>
    </div>
}
    <div class="vehicle-call-to-action mr-2">
        <a>
            <small>
                Sản Phẩm Có
            </small> Hỗ Trợ Trả Góp
        </a>
    </div>
<div class="clear-float"></div>